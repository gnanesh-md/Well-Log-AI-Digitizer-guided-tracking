# Local CPU-only configuration (OCR removed)
# Model weights are optional - the code has density-based fallbacks if missing.
YOLO_MODEL_PATH=models/extract_header_model.pt
TIFF_CHUNK_MODEL_PATH=best.pt
USE_UNET_PIPELINE=false
